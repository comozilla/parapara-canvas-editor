// frame の追加・削除と、DOM上でのCanvasの追加・削除を連動させる

function FrameServer(elem) {
  
}

FrameServer.prototype.append = function() {
  
};

FrameServer.prototype.remove = function() {
  
};

module.exports = FrameServer;